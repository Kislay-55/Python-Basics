first_name = 'Asabeneh'
last_name = 'Yetayeh'
language = 'Python'
formated_string = 'I am %s %s. I teach %s' %(first_name, last_name, language)
print(formated_string)
first_name ='Kislay'
last_name = 'Sinha'
language = 'Python'
formated_string = 'I am %s %s and i teach %s' %(first_name , last_name , language)
print(formated_string)
radius = 10
pi = 3.14 
area_of_circle = pi * radius ** 2
formated_string = 'The area of a circle with radius %d is %.2f.'
str1 = ' '.join(['Thirty', 'Days', 'Of', 'Python'])
str2 = ' '.join(['Coding', 'For', 'All'])
company = "Coding For All"
print(company)
print(len(company))
upper_company = company.upper()
lower_company = company.lower()
cap_company = company.capitalize()
title_company = company.title()
swap_company = company.swapcase()
first_word = company.split()[0]
contains_coding = 'Coding' in company
replaced_company = company.replace('Coding', 'Python')
replaced_everyone = "Python for Everyone".replace("Everyone", "All")
split_space = company.split(' ')
split_comma = "Facebook, Google, Microsoft, Apple, IBM, Oracle, Amazon".split(', ')
char_at_0 = company[0]
last_index = len(company) - 1
char_at_10 = company[10]
acronym_pfe = ''.join([word[0] for word in 'Python For Everyone'.split()])
acronym_cfa = ''.join([word[0] for word in 'Coding For All'.split()])
index_c = company.index('C')
index_f = company.index('F')
rfind_l = 'Coding For All People'.rfind('l')
sentence = 'You cannot end a sentence with because because because is a conjunction'
find_because = sentence.find('because')
rindex_because = sentence.rindex('because')
start_slice = sentence.find('because')
end_slice = sentence.rindex('because') + len('because')
sliced_phrase = sentence[start_slice:end_slice]
first_because_pos = sentence.find('because')
starts_with_coding = company.startswith('Coding')
ends_with_coding = company.endswith('coding')
stripped_str = '   Coding For All      '.strip()
is_valid_identifier = 'thirty_days_of_python'.isidentifier()
joined_libraries = '# '.join(['Django', 'Flask', 'Bottle', 'Pyramid', 'Falcon'])
print("I am enjoying this challenge.\nI just wonder what is next.")
print("Name\tAge\tCountry\tCity\nAsabeneh\t250\tFinland\tHelsinki")
radius = 10
area = 3.14 * radius ** 2
print(f"The area of a circle with radius {radius} is {int(area)} meters square.")
print(f"8 + 6 = {8 + 6}")
print(f"8 - 6 = {8 - 6}")
print(f"8 * 6 = {8 * 6}")
print(f"8 / 6 = {8 / 6:.2f}")
print(f"8 % 6 = {8 % 6}")
print(f"8 // 6 = {8 // 6}")
print(f"8 ** 6 = {8 ** 6}")